/*import React from 'react'

interface ListProps  {
  tasks: String[]
}
interface ListItem {
  task: String
}

export  const  List = ({tasks}: ListProps) => {

  const removeListItem = ({task}: ListItem) => {
    setTasks
  }
  return (
    <ul>
      {tasks.map(task => (
        <li key={task.toString()}>
          {task}
          <button onClick={}>remove</button>
        </li>
      ))}
    </ul>
  );
}
*/
export {}
